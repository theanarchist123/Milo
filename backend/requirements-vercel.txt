fastapi
uvicorn[standard]
sqlalchemy
python-dotenv
python-multipart
pydantic
apscheduler
requests
beautifulsoup4
python-docx

# Gemini AI (new SDK — replaces deprecated google-generativeai)
google-genai

# Google API access (Gmail, Classroom, Drive + Firebase token verification)
google-api-python-client
google-auth>=2.0.0
google-auth-httplib2
google-auth-oauthlib

# PostgreSQL driver (used on Vercel / production)
psycopg2-binary

# NOTE: firebase-admin is NOT required.
# We use google.oauth2.id_token.verify_firebase_token (part of google-auth)
# which verifies Firebase JWTs using Google's public keys — no service account needed.

# NOTE: playwright is intentionally excluded here.
# It requires browser binaries (~300MB) and cannot run on Vercel serverless.
# browser_scraper.py falls back to requests + BeautifulSoup automatically.
